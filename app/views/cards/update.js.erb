<% if params[:card].length > 1 %>
  <% broadcast "/listener" do %>
    $("#<%= @card.id %>").replaceWith("<%= escape_javascript render(@card) %>");
    window.dragableInitCards();
  <% end %>
<% end %>